name: hello_world
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2  # <--- Agora no lugar certo!
  geolocator: ^14.0.3
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  flutter_blue_plus: ^2.0.3
  sensors_plus: ^7.1.0
  audioplayers: ^6.8.1
  image_picker: ^1.2.3
  sqflite: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/audio/som.mp3
  uses-material-design: true
  # Removi os assets que estavam dando erro de "directory not found"
